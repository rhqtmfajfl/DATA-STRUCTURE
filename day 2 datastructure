day 2 

1210


추상화 이객체를 어떻게게 쓸 것인가?

추상화는 데이터가 어떻게 흘러 가는지 잘 알아야 한다.

캡슐화 데이터 은닉 데이터 보호 이데이터를 어덯게 관리할 것인가? 
나를 통하지 않고 변경할 수 없다.

데이터베이스는 

상속은 물려받는 것이다. 상속관계를 명확하게 해야 한다.


상속관계를 명확하게 해야 한다. .....

다형성 : 

알고리즘은 문제해결 능력을 의미 한다. 내가 문제를 해결하기위해 어떻게 할까? 가장 효율적으로 문제를 해결한다.


추상 인어 클래스


인터페이스는 다중 상속을 대체 할 수 있다. 현재 확장성이 좋아진다. 

인터페이스는 기능을 가지고 있다. 복잡성이 발생할 수 있다.

추상메소드는 강제성을 가지고 있다. // abstract를


코드를 보고 확장성 유지보수성을 생각해야 한다.


//////////

protected int[] data = new int[5]; 상수로 사용할때 사용하므로 좋은 코드는 아니다.  -> new int[5] 생성자에서 만들어준다.  생성자에서?? 생성자에서 빼두는 습관을 가진다.

prtected int index =0; 처음부터 시작하고 싶으면 생성자에서 따로 빼놓는다.

private은 메소드를 통해서만 쓸수 있다.
private를 쓸려면 

밑에서 상속 받은 자식들도 free하게 쓰기위해서 protected를 쓴다.


push는 filo
fifo



int를 return하면 정수를 리턴하므로 boolean을 사용하기 위해서는 논리적으로 문제가 있는지 없는지 보겠다.

memory memory = null; 객체가 없을때 널값 넣는다.




///////////////////////////////



inner class 는 말그대로 클래스 안에 클래스가 되어있는 것을 말한다.


has a관계는 독립적으로 클래스를 사용한다?


inner 클래스는 클래스안의 클래스를 따로 독립적으로 사용할 수는 없다.

outer클래스에 최적화 되어있다는 얘기는 내것 처럼 사용할 수 있다.


필드값 레퍼런스 사용할 ㅅ ㅜ있다.

이너 클래스는 아우터에 있는 멤버를 내것 처럼 사용할 수 있다 아우터의 private도사용 가능하다는 의미

메소드는 확장자의 개념이다.


inner class는 4가지가 있다.



1. Member inner class
  클래스 이너안에 클래스 이너라고 만들면 멤버 이너 클래스다
2. static inner class
 이너 클래스 앞에 스태틱을 붙이면 된다.

3. local inner class

4. annoymous class


위의 4가지 구별


public을 사용하는 것은 다른 클래스에서도 접근 할 수 있게 하는 것 이다.

1.과 2.의 큰차이점은 스태틱 메소드는 메모리가 생성되는 시점이 다르다.

이너 클래스는 아우터클래스를 만들고 이너를 만들 수 있다.

스태틱은 미리 메모리에 만들기 때문에 

스태틱 이너 메소드는  아우터에 접근 할 수 있다.

아우터 스태틱 필더는 객체를 생성하지 않아도 만들어진다.

스태틱 이너는 아우터의 스태틱 필드를 만들기 위해서다.

로컬 이너 클래스는 아우터 메소드 안에 변수를 만드는것



4. 어나니머스 이너 클래스  이름이 없는 클래스 단발성으로 이벤트성을 사용할 때 어나니머스 이너 클래스를 사용한다.



1. 과 4.를 가장 만이 사용한다.

그중에 4를 많이 사용 이유는 이벤트 처리시 좋다. 코드가 깔끔해진다.




///////////////////////////


제네릭

리스트는 데이터를 순차적이나 삽입 할 때 리스트를 사용한다.

리스트 - 어레이 리스트 링크드 리스트 , 벡터

어레이 리스트가 벡터의 서브 클래스이다. 

어레이 리스트가 하이리스트이다. 서브 클래스라는 이야기는 부모 것을 물려 받고 더 개선 향상 된것

어레이리스트와 벡터의 차이점

동기화 - 벡터 : 동기화 한다.

동기화 x - 어레이 리스트 : 동기화 안한다. 다른 클래스의 도움을 받아서 동기화를 할 수도 있다.



어레이 리스트는 단순히 배열이다.  

링크드 리스트는 배열이 아니다.   ************ 링크드 리스트 시험**********


어레이 리스트는 

링크드 리스트는 노드를 연결해 되어있는것 

링크드 리스트를 쓰면 중간에 삽입삭제가 어레이 리스트보다 훨씬더 빠르다.


중간에 삭제해도 shift를 하지 않아도 된다.
삽입을 할때는  1번 2번 3번 노드에서 2번 노드에 새롭게 노드를 쓰면 1.1번이 들어가면  1번과 2번 사이에 다시 들어가서 연결된다.


///////
set은 중복된 데이터를 허용하지 않고 순서가 없다.


set의 단점  작업량이 많다.



/////////////////


map은 key하고 value로 구성되어있다.

key는 중복이 허용이 안되지만 value는 중복이 허용 된다.

-> 목적에 맞게끔 map에 쓰거나 set을 쓰거나 사용해야 한다.


////////////////////////////////////

쓰레드 ...

process 실행되고 있는 프로그램

프로세스를 여러개 쓰면 멀티 태스킹이다.

쓰레드 프로세스안에서 돌고있는 작은 프로세스 백그라운드에서 일을 한다.

예시 쓰레드 기능을 사용하는 카카오톡

같은 자원을 동시에 쓸때는 쓰레드에서 문제가 발생한다.

예시 작업1과 작업2 작업 3이 있는데 작업1과 작업2가 작업을 할때 중간에 작업3이 작업을 하면 문제가 발생한다.

그래서 이때 동기화 처리를 해주면 된다.

자바에서는 동기화 블록과 동기화 메소드를 사용한다.

자바에서는 쓰레드 클래스가 있다.


쓰레드 하나 쓰면 단일 쓰레드

멀티쓰레드 여러 쓰레드 사용하는데 이때 동기화 처리를 해줘야 한다.

자바에서는 쓰레드 기능을 제공해준다.

자바는 다중 상속이 안되기 때문에 쓰레들르 쓸수 있는 

runnable을 사용하면 상속을 받는 것을 많이 사용한다.



쓰레드는 공정하게 처리 해야 하고 기아상태가 발생하면 안된다.

웨이트 노티파이 노티파이 올?


쓰레드는 중간에 멈출 수가 없이만 다른 함수를 사용하면 된다.
